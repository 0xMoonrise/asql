#!asql -f
SELECT m#,
       mnombre
FROM   materias
WHERE  m# IN
       (
              SELECT m#
              FROM   inscritos
              WHERE  a# IN
                     (
                            SELECT a# &
                            FROM   alumnos
                            WHERE  anombre='MESSI LIONEL'))
